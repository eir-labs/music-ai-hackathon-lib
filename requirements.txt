# The shared library, plus every track's dependencies. Installing this way
# also puts the `kitlib` command on your path.
# Travelling light? `pip install -e ".[radar]"` takes only what your track needs.
-e .
python-osc
waapi-client
acconeer-exptool
pyserial
numpy
mido
python-rtmidi
